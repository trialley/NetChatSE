var app = new Vue({
	el: '#lgapp',
	data: {
		toid: "hhh"+Math.ceil(Math.random() * 100),
		
	}
})







var voice_connecter = new Voice_connecter()
// voice_connecter.login('lg')
// voice_connecter.startwith("zjs")
// voice_connecter.close()